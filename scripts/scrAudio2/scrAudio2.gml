if (room = Room1)
{
audio_sound_gain(M_Birds_and_regular_ambiance,0,5000);

audio_sound_gain(M_Morning, 0 ,5000);

audio_sound_gain(M_Night, 0 ,5000);

audio_sound_gain(M_Night,0.42 ,10000);

audio_sound_gain(M_Storm, 0 ,5000);

audio_sound_gain(M_Wet, 0 ,5000);

audio_sound_gain(M_Windier, 0 ,5000);
}