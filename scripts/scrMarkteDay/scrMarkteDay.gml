if (room == Room1)
{
	if (obj_Calander.isItMarketDay = true) && (obj_TimeControl.day = true)
{
	Love += (1 * LoveMult)
	
}

	if (obj_Calander.isItMarketDay = true) && (obj_TimeControl.night = true) && (obj_RootControler.NightMarkets = true)
	{
		Love += (1 * LoveMult)
	}



if (obj_Calander.isItMarketDay = false)
{
	Love--;
}
}